{
  "tasks": {
    "dev": "deno run -A npm:vite",
    "build": "deno run -A npm:vite build",
    "preview": "deno run -A npm:vite preview",
    "fmt": "deno fmt",
    "lint": "deno lint"
  },
  "compilerOptions": {
    "lib": ["DOM", "ESNext"],
    "strict": true
  }
}
